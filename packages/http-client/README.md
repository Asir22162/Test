# packages/http-client

Typed HTTP client with production features:

- Retries with exponential backoff
- Timeouts and concurrency limits
- Pluggable adapters (fetch, axios, node http)
- Automatic instrumentation hooks (metrics, tracing)