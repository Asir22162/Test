# packages/metrics

Minimal metrics SDK wrapper for counters/gauges/histograms and export to Prometheus / other backends.

Include instrumentation conventions for request duration, error counts, and business events.