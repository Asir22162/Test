{
  "name": "ops",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "vitest",
    "test:ops": "vitest tests --run",
    "test:ops:watch": "vitest tests --watch",
    "check-secrets": "node ops/check-secrets.js"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "nock": "^13.3.0",
    "@types/node": "^18.0.0"
  }
}