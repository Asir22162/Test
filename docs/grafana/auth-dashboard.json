{
  "annotations": {
    "list": []
  },
  "editable": true,
  "gnetId": null,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "type": "timeseries",
      "title": "Auth: Revokes (rate)",
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "targets": [
        { "expr": "rate(auth_revoke_count[5m])", "legendFormat": "revokes/sec" },
        { "expr": "rate(auth_revoke_rate_denied[5m])", "legendFormat": "denied/sec" }
      ]
    },
    {
      "type": "timeseries",
      "title": "Auth: Rotations & Issues (rate)",
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "targets": [
        { "expr": "rate(auth_issue_count[5m])", "legendFormat": "issues/sec" },
        { "expr": "rate(auth_rotate_count[5m])", "legendFormat": "rotate/sec" },
        { "expr": "rate(auth_rotate_failures[5m])", "legendFormat": "rotate-failures/sec" }
      ]
    },
    {
      "type": "stat",
      "title": "Revoke Denied (5m rate)",
      "gridPos": {"h": 4, "w": 6, "x": 0, "y": 8},
      "targets": [{ "expr": "sum(rate(auth_revoke_rate_denied[5m]))" }]
    },
    {
      "type": "stat",
      "title": "Revoke Allowed (5m rate)",
      "gridPos": {"h": 4, "w": 6, "x": 6, "y": 8},
      "targets": [{ "expr": "sum(rate(auth_revoke_rate_allowed[5m]))" }]
    },
    {
      "type": "stat",
      "title": "Redis Errors (5m)",
      "gridPos": {"h": 4, "w": 6, "x": 12, "y": 8},
      "targets": [{ "expr": "sum(rate(auth_revoke_rate_redis_errors[5m]))" }]
    },
    {
      "type": "timeseries",
      "title": "Token Bucket Remaining (per-user sample)",
      "gridPos": {"h": 4, "w": 18, "x": 0, "y": 12},
      "targets": [{ "expr": "auth_tokenbucket_remaining" }]
    }
  ],
  "schemaVersion": 36,
  "style": "dark",
  "tags": ["auth","revoke","tokens"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "Auth - Token & Revoke Metrics",
  "uid": "auth-token-revoke-dashboard",
  "version": 1
}
