# Missing Items — Issue list & priorities

This file enumerates missing items found during the foundation-layer review and proposes Issue templates (title, priority, labels, brief description, acceptance criteria) so maintainers can quickly create tracker issues.

---

## 1) ADR: Decide backend framework & ORM (P0) ⚠️
- Labels: architecture, decision, priority/P0
- Description: Decide whether to standardize on **NestJS + TypeORM** (recommended in initial proposal), **NestJS + Prisma**, or another combination. Produce an ADR that compares options, lists migration costs, and provides a recommended path.
- Acceptance criteria:
  - ADR file committed to `docs/architecture/` (name: `0003-backend-framework-orm.md`).
  - Stakeholders reviewed and signed off (e.g., @team-backend, @team-frontend).
  - A follow-up implementation plan (scaffold, examples, migration checklist) is created as Issues.

---

## 2) Frontend constraint: Lock to native WeChat Mini Program (P1) ✅
- Labels: docs, policy, priority/P1
- Description: Add explicit documentation in `CONTRIBUTING.md` and `docs/architecture/*` that **only native WeChat Mini Program (WXML + WXSS + JS/TS)** is allowed; uni-app and similar cross-compilers are disallowed or need explicit approval.
- Acceptance criteria:
  - `CONTRIBUTING.md` updated with the constraint.
  - README/APP-level docs updated for `apps/consumer-weapp` and `apps/merchant-weapp` to reference the constraint.

---

## 3) Payment PoC: WeChat Pay v3 simulator (P1)
- Labels: feature, payments, priority/P1
- Description: Implement a local/simulated WeChat Pay v3 PoC in `libs/payment` or `packages/payment` showing basic flow (create prepay, simulate payment notification callback, idempotency and reconciliation hooks). Keep external dependency minimal (mocked HTTP responses). Document test vectors.
- Acceptance criteria:
  - Example flow implemented and test-covered (unit + integration with a small HTTP server). 
  - Documentation (README + example) showing how to run PoC locally.

---

## 4) COS placeholder (P2)
- Labels: infra, storage, priority/P2
- Description: Add a `libs/storage` or `packages/storage` interface that defines the minimal contract for object storage (upload, download, signed urls, delete). Provide a noop/local filesystem implementation and a COS adapter stub that can be implemented later.
- Acceptance criteria:
  - `packages/storage` scaffolded with interface and local adapter.
  - README describes how to implement a COS adapter and where to add config (env variables).

---

## 5) (Optional) Follow-up: Add CI checks to ensure these decisions are enforced
- Labels: ci, automation
- Description: Add linter or repo checks (e.g., package.json scanning) that warn/error when disallowed frontend frameworks are introduced, or when new services are implemented without using the chosen ORM pattern.

---

### How to use
- Copy the sections above into your issue tracker (GitHub Issues) or create a single umbrella issue and link each sub-issue. If you'd like, I can also create the GitHub Issues automatically (requires a PAT or you can run the provided `gh issue create` commands locally).

---

*Generated by automation for clarity and to speed up project onboarding.*
