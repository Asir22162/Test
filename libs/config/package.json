{
  "name": "@weixin/config",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint . --ext .ts",
    "test": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}